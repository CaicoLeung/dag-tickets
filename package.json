{
  "name": "loop-tickets",
  "version": "0.1.0",
  "description": "DAG-aware batch driver for mattpocock-skills tickets: implement -> code-review -> fix-loop -> PR -> auto-merge, fanned out across Paseo worktrees.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "loop-tickets": "./bin/loop-tickets.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "loop-tickets": "bun run bin/loop-tickets.ts"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
